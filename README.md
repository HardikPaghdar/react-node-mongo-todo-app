# react-node-mongo-todo-app

## BACKEND

Please add mongodb DB_URI in backend/.env file. 

In the backend directory, you can run:

### `npm install`
### `npm start`

## FRONTEND

In the frontend directory, you can run:

### `npm install`
### `npm start`

